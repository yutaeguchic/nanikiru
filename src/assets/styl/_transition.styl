.fadeDown-enter-active {
  animation fadeDown .5s
}
.fadeDown-leave-active {
  animation fadeDown .5s reverse
}
@keyframes fadeDown
  0%
    opacity 0
    transform translate3d(0, -100%, 0)
  100%
    opacity: 1
    transform none